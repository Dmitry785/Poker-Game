<Window x:Class="Poker.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Poker"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <TextBlock x:Name="pageName" Padding="20"/>
        </Grid>
        <Grid Grid.Row="1">
            <ContentControl x:Name="front"/>
            <ContentControl x:Name="back" Visibility="Collapsed"/>
        </Grid>
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Content="Game" Tag="0" Click="PageButton_Click" 
                    Grid.Column="0" Template="{StaticResource PageButtonStyle}"/>
            <Button Content="Info" Tag="1" Click="PageButton_Click" 
                    Grid.Column="1" Template="{StaticResource PageButtonStyle}"/>
            <Button Content="Settings" Tag="2" Click="PageButton_Click"
                    Grid.Column="2" Template="{StaticResource PageButtonStyle}"/>
        </Grid>
    </Grid>
</Window>
