<UserControl x:Class="Poker.Views.Player"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Poker.Views"
             mc:Ignorable="d" 
             d:DesignHeight="120" d:DesignWidth="80">
    <UserControl.Resources>
        <local:BoolToVisibility x:Key="boolToVisibility"/>
        <local:PlayerStatuslToColorConverter x:Key="statusToColor"/>
        <local:MoneyPresentConverter x:Key="MoneyConverter"/>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Margin="5">
            <!--
            <Grid.Background>
                <ImageBrush ImageSource="/data/player.png" Stretch="Uniform"/>
            </Grid.Background>
            -->
            <Border Grid.Row="0"
                            Width="60" 
                            Height="60" 
                            CornerRadius="30"
                            ClipToBounds="True"
                            Margin="5"
                            Background="{Binding Status, Converter={StaticResource statusToColor}}">
                <Image Source="/data/player.png" 
                                Stretch="Uniform">
                    <Image.Clip>
                        <EllipseGeometry Center="30, 30"
                         RadiusX="28"
                         RadiusY="28"/>
                    </Image.Clip>
                </Image>
            </Border>
            <Border Grid.Row="0"
                            Width="20" 
                            Height="20" 
                            CornerRadius="10"
                            ClipToBounds="True"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Bottom"
                            Margin="5"
                            Visibility="{Binding IsDealer, Converter={StaticResource boolToVisibility}}">
                <Image Source="/data/dealer.png" 
                    Stretch="Uniform">
                    <Image.Clip>
                        <EllipseGeometry Center="10, 10"
                         RadiusX="10"
                         RadiusY="10"/>
                    </Image.Clip>
                </Image>
            </Border>
        </Grid>
        <StackPanel Grid.Row="1">
            <Border BorderBrush="Black" BorderThickness="1" CornerRadius="5">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0, 0" EndPoint="1,1">
                        <GradientStop Color="Blue" Offset="-0.3" />
                        <GradientStop Color="White" Offset="0.5" />
                        <GradientStop Color="Blue" Offset="1.3" />
                    </LinearGradientBrush>
                </Border.Background>
                <TextBlock Text="{Binding Name}" TextAlignment="Center" 
                                    FontSize="12" FontWeight="Bold"/>
            </Border>
            <Border BorderBrush="Black" BorderThickness="1" CornerRadius="5">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0, 0" EndPoint="1,1">
                        <GradientStop Color="Orange" Offset="-0.3" />
                        <GradientStop Color="WhiteSmoke" Offset="0.5" />
                        <GradientStop Color="Orange" Offset="1.3" />
                    </LinearGradientBrush>
                </Border.Background>
                <TextBlock Text="{Binding Money, Converter={StaticResource MoneyConverter}}" TextAlignment="Center" 
                                    FontSize="12" FontStyle="Italic"
                                    Foreground="Green"/>
            </Border>
            <Border BorderBrush="Black" BorderThickness="1" CornerRadius="5" Visibility="{Binding CurrentMoveVisibility}" >
                <Border.Background>
                    <LinearGradientBrush StartPoint="0, 0" EndPoint="1,1">
                        <GradientStop Color="LightBlue" Offset="-0.3" />
                        <GradientStop Color="BlueViolet" Offset="0.5" />
                        <GradientStop Color="LightBlue" Offset="1.3" />
                    </LinearGradientBrush>
                </Border.Background>
                <TextBlock Text="{Binding CurrentMove}" TextAlignment="Center" 
                        FontSize="12"/>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>
